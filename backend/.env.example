# Copy to .env — ONE active DB_URL line only, no spaces around =
# Use Atlas on Windows/other devices (get string from Atlas → Connect → Drivers):
DB_URL=mongodb+srv://USER:PASSWORD@hussain.7ilwz7z.mongodb.net/hussain?retryWrites=true&w=majority

# Local MongoDB only if MongoDB is INSTALLED and RUNNING on this same machine:
# DB_URL=mongodb://127.0.0.1:27017/hussain

# Email — on Render FREE tier, SMTP (ports 587/465) is blocked. Use SendGrid instead:
# SENDGRID_API_KEY=SG.xxxx
# MAIL_FROM=verified-sender@yourdomain.com   (must match SendGrid verified sender)

# Or SMTP (works locally and on Render PAID plans):
MAIL_FROM=
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_EMAIL=
MAIL_PASSWORD=
JWT_SECRET=
FRONT_APP_URL_DEV=http://localhost:5173
PASSPORT_SECRET=
BACKEND_URL=http://localhost:3000
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
PORT=3000
